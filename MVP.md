# Sentinel-Mobile MVP 路线图

## 0. 固定开发纪律

- 严格按 `MVP V1 -> MVP V2 -> MVP V3 -> MVP V4` 的顺序推进，不跳级开发。
- 每次完成一个 MVP 阶段或阶段性里程碑后，必须执行以下动作：
  1. 由技术总监 Agent 对照本文件的 DoD 做完成度审查。
  2. 运行本阶段受影响的后端、前端与文档验证。
  3. 同步更新根目录 `README.md`。
  4. 把阶段完成情况回填到本文件的“当前阶段判断”和对应 MVP 小节。
  5. 执行 Git 同步：`git status -> git add -> git commit -> git push`。
- `work_now.md` 是 Codex 当前开发的唯一执行流程文档；`work.md` 已废弃并删除。

## 1. 当前阶段判断（基于当前代码，更新时间：2026-04-06）

### 项目总判断

注：
一、当前代码存在问题，你在开发下一个版本前，要解决这个问题，分析项目架构和代码告诉我问题处在了哪里：
1.agent好像有语义识别功能，但只是关键词 的匹配，对于一些错误信息无法有效识别，对一些语义信息无法有效提取
二、下一阶段对于V1,2的优化，你将这些优化内容整理到之际的工作计划中，并合理编排工作计划：
1.优化风险数据库，增加风险类型分类，增加用户数据，完善用户画像，增加常见风险场景关键词；
2.说明内容应该根据当前风险类型进行针对性提问。
三、回答我的问题，现在我要：
1.弄清楚当前分类逻辑和语义关键词匹配逻辑。
2.弄清楚静态风险分如何得到，当前代码中数据是如何流动的；
3.弄清楚行为风险、外部情报的如何得到，他的处理流程。
四、新增全局要求：
每次生成一段代码，同时附上“代码职责说明书”，注释每一个函数在 HIRD 模型四层架构中属于哪一层。每次完成一个版本开发都将各个函数的解释整合到Showme_func.md中

- 当前项目处于：`MVP V3 开发中`。
- 换成业务阶段来理解：
  - 阶段 1 无感监测：已完成。
  - 阶段 2 风险提示：已完成。
  - 阶段 3 受控质询：已完成基础闭环，进入语义升级阶段。
  - 阶段 4 治理闭环：仅完成早期雏形。

### 已落地能力汇总

- 三态风控决策：`pass / interrogate / block`。
- 风险分解落库：`flag_s / g_behavior / g_dynamic / final_risk`。
- 风险知识库分类：已支持常见金融风险场景、关键词命中、追问建议、标准回答样例。
- 二次拦截主链路：已支持 `secondary-check` 接口和 `pass_secondary / block_secondary` 二次决策。
- 模型调用路径：已统一到 OpenAI 兼容字段。
- Flutter 风控交互：已标准化账单弹窗与二次质询交互。
- 代码质量治理：已完成 HIRD 注释清理、文本归一化统一、二次拦截对象结构化及 Decision 枚举重构。
- 测试口径：已统一使用 `北京 / 上海 / 西安` 与 `小a-f` 作为主要演示样例。

### 当前阶段完成度（汇总自旧 work.md 并按当前代码修正）

- 阶段 1 无感监测：`95%`。
  - 已完成：预检、评分、落库、基础上下文采集、文本归一化统一。
  - 未完成：微观行为脉冲建模。
- 阶段 2 风险提示：`100%`。
  - 已完成：风险提示、分类结果、账单与风险弹窗主交互、XAI 解释面板、外部情报适配。
- 阶段 3 受控质询：`90%`。
  - 已完成：二次质询接口、用户补充说明、差异化追问、语义红旗阻断、返回结构化对象。
  - 未完成：语义检索升级（V3 核心）、多轮质询。
- 阶段 4 治理闭环：`40%`。
  - 已完成：`Decision` 枚举隔离、审计字段。
  - 未完成：硬策略引擎隔离、执行控制器、多 Agent 运行时闭环。

## 2. MVP V1：认知核与风险分类（Cognitive Core & Intent Classification）

### 目标

围绕转账场景先打通“风险分类 -> 预检决策 -> 二次质询 -> 二次放行/拦截”的最小闭环。

### 当前状态

- 状态：`已完成`。

### 已完成

- 已建立轻量风险知识库，覆盖熟人借钱、冒充公检法、安全账户、客服退款、验证码/屏幕共享、异地大额异常转账等场景。
- 已统一 DeepV3 / OpenAI 兼容接口调用路径，移除本地 mock 依赖。
- 已实现风险融合评分与三态决策。
- 已完成前端最小 UX 收口：删除首页“最近交易”、账单弹窗化、二次质询标准化。
- 已完成基础回归测试样例统一。

### 未完成但不阻塞进入 V2 的增强项

- 将当前固定加权融合，进一步升级为带 `alpha_final` 的动态权重版本。
- 把知识库检索从轻量关键词匹配继续增强为更稳定的向量检索/召回机制。

### DoD

- [x] 能对“异地转账”“熟人借钱”等场景输出结构化风险分类结果。
- [x] 测试样例统一为 `北京 / 上海 / 西安` 与 `小a-f`。
- [x] 二次质询具备标准回答样例。
- [ ] 风险融合公式尚未完全对齐到 MVP 里的动态权重形态。
- [x] 阶段文档治理与 README 同步流程已在 V2 收口中执行验证。

## 3. MVP V2：感知集成与可解释看板（Perception & XAI）

### 目标

把已有分类和决策结果变成“可解释、可展示、可复核”的风控现场，让用户和评审都能看懂 Agent 为什么怀疑当前交易。

### 当前状态

- 状态：`已完成，允许在 Git 同步后切换到 V3`。

### 开发顺序

1. 先补强内部感知上下文：统一 `ClientContext`、KYC、地理位置、设备与行为信号的接入边界。
2. 再补最小 XAI 看板：把知识库命中、静态分、行为分、动态分和最终风险分可视化给前端。
3. 最后补外部情报工具适配层：为 `check_sanctions_watchlist`、负面信息查询等能力预留工具接口。

### 已完成

- `ClientContext` 已能承载城市、页面、动作、语义摘要、输入停顿、输入时长和扩展信号。
- 银行 MCP 已存在，可作为感知层工具化入口。
- 后端已为 `precheck` 与 `secondary-check` 返回结构化 `explain_pack`。
- 后端已为 `precheck` 与 `secondary-check` 返回结构化 `external_intelligence`。
- 前端已在风险拦截、二次质询、二次拦截结果弹窗中接入 XAI 解释面板。
- 前后端关于 `ExplainNode` 的合同已统一为 `id / title / level / summary / detail / score`。
- 已接入最小外部情报适配层 `watchlist_adapter_demo`，支持收款人名单/负面情报命中。
- `explain_pack` 已新增 `external_intelligence` 节点，MCP 已新增 `screen_external_intelligence` 工具。
- V2 对应的后端 API 测试、Flutter widget/data contract 测试已补齐。
- V2-patch-1 已完成：风险知识库新增投资理财诈骗、情感诈骗、刷单兼职诈骗，支持 `high_risk_phrase_hits` 与空语义兜底分类。
- V2-patch-2 已完成：二次质询主问题按场景动态选择，并把强制核验点注入 Agent 提示词。
- V2-patch-3 已完成：二次质询新增语义红旗直拦、无关回复不得放行、`semantic_red_flags` 响应字段。

### 未完成

- 解释节点当前仍是最小面板版，尚未扩展为更完整的组件拓扑/思维链展示。
- V3 所需的微观行为建模、隐私护栏与治理控制面尚未启动。

### DoD

- [x] UI 能清晰展示“为何怀疑本次交易”的解释面板。
- [x] 后端向前端稳定返回解释包，而不只是零散原因文本。
- [x] 至少完成一个外部情报工具的接口适配或占位实现。

### V2 收口验证

- `server/tests/test_api.py`：14 通过
- `server/tests/test_risk_scenarios.py`：13 通过
- `server/tests/test_patch2_secondary_followup.py`：2 通过
- `flutter analyze --no-version-check`：通过
- `flutter test --no-version-check`：6 通过

## 4. MVP V3：微观行为感知与隐私护栏（Behavioral S3 & Privacy Guard）

### 目标

增强微观行为风控，并确保进入认知层的数据满足最小化、脱敏和可审计要求。

### 当前状态

- 状态：`早期预埋，尚未进入主开发`。

### 已完成

- 已有输入停顿、输入时长、扩展信号的基础采集框架。

### 未完成

- 未形成真正的 S3 行为脉冲模型。
- 未实现 PII 脱敏与重水化机制。
- 未接入监管通告或合规建议工具。

### DoD

- [ ] 能识别“受迫操作”类微观行为异常并参与决策。
- [ ] Agent 推理日志不暴露明文敏感信息。
- [ ] 合规提示可基于外部规则源动态调整。

## 5. MVP V4：治理闭环与多 Agent 执行（Governance & Swarm Execution）

### 目标

实现分析与裁决分离，把运行时多 Agent 协作真正纳入治理控制面，而不是只做开发期并行。

### 当前状态

- 状态：`未开始，仅有前置约束雏形`。

### 已完成

- 二次决策已有 `policy_version` 和审计字段。
- `block` 路径已禁止直接确认转账。

### 未完成

- 尚未实现独立于 LLM 的硬策略引擎隔离层。
- 尚未实现执行控制器、人工复核分流和运行时多 Agent 编排。
- 尚未完成大规模压力验证与提示注入防御验证。

### DoD

- [ ] 存在独立的硬策略引擎，能兜住最终拦截红线。
- [ ] 存在执行控制器，能落实拦截、转人工或限制操作。
- [ ] 完成运行时多 Agent 治理闭环和压力测试。
