# Banking AI Project · 架构总览与开发 TODO（V3 -> V5）

> 文档用途：集中记录项目完整度评估、当前架构、当前阶段要求与后续开发 TODO。  
> 执行计划仍以 [plan.md](e:/Projects/Banking_AI_Project/plan.md) 为准，本文件用于跨版本对齐与工程落地跟踪。

## 1. 当前完整度评估（2026-04-08）

### 验证基线
- 后端核心回归：`46 passed`
- 前端静态检查：`flutter analyze` 通过
- 前端关键交互回归：`widget_test.dart` 全通过
- 本地性能基线：`precheck avg 29.66ms / p95 80.60ms`，`secondary-check avg 43.99ms / p95 108.76ms`

### 功能完整度
| 维度 | 状态 | 说明 |
|---|---|---|
| 预检风控主链路（pass/interrogate/block） | 已完成 | 含风险公式、阈值路由、硬拦截优先级 |
| 二次质询单轮策略 | 已完成 | 同 token 仅一次 secondary-check，未通过不可 confirm |
| 显式取消交易 | 已完成 | `POST /api/v1/transfers/cancel` + 前端显式入口 |
| 三类 Agent 分工（初分/细分/语义） | 已完成（V3 形态） | 已在服务中固化职责，治理层保留最终裁决 |
| 语义否定识别（如“不涉及验证码”） | 已完成（规则增强） | 已避免简单关键词误判 |
| Trace 审计落库与监管视图 | 部分完成 | 已有事件记录，未形成独立审计通道与报表 |
| 图式编排（LangGraph/LangChain） | 未开始 | 作为后续可选演进（非 V4/V5 必做） |

**结论**：当前可视为 **V3 可交付版本（约 80%~85%）**，已具备端到端演示与回归稳定性；下一阶段关键增量在取消交易全链路指标化、Trace 审计写入 MCP，以及感知层/治理层能力补全。

## 2. 当前项目架构（HIRD 映射）

### HIRD-H 感知集成层
- 上下文输入：城市、设备、页面动作、交易金额、语义摘要。
- 外部情报筛查：收款人命中负面名单时升级风险。

### HIRD-C/R 认知决策层
- `RiskKnowledgeBase`：向量召回 + 规则打分 + 否定识别。
- `AgentService`：二次质询语义拦截（红旗直拦、低风险本地放行、LLM 兜底）。
- 三类职责固定：
  - 领域初分：场景定性与首问方向。
  - 领域细分：剧本匹配与 `c_match` 信号。
  - 语义分析：红旗信号与解释充分性判定。

### HIRD-G 治理控制层
- `RiskEngine` 执行确定性裁决。
- 数学公式：
  - `w_adj = w_base + (1 - w_base) * c_match`
  - `f_final = (1 - w_adj) * s_static + w_adj * s_dev`
- 阈值路由：`<0.3 pass`、`0.3~0.8 interrogate`、`>0.8 block`。
- 硬拦截命中时直接 `block`。

### HIRD-E 业务执行层
- 交易状态：`pending -> committed/cancelled`。
- 二次质询策略：单轮一次提交，非 `pass_secondary` 不允许确认。

## 3. 当前阶段要求（V3 口径）

- 保持 FastAPI + 手写调度，不切 LangGraph/LangChain 主编排。
- 保持对外接口语义稳定：`precheck / secondary-check / cancel / confirm`。
- 保持“Agent 只分析，治理层裁决”。
- 无效输入统一文案：`无效输入，请按照要求输入`。
- 取消交易必须是显式业务动作，不与关闭弹窗混淆。

## 4. 版本进度同步

| 版本 | 目标 | 状态 |
|---|---|---|
| V1 | 初版规则风控闭环 | 已归档 |
| V2 | 场景扩展与可解释输出 | 已完成 |
| V3-alpha | 交互治理收口 + 三类 Agent 契约 + 单轮二次质询 | 已完成（当前运行版本） |
| V3-beta | 取消交易全链路统一 + 指标化 + Trace 审计写入 MCP | 待开发 |
| V4 | 完善 MCP 感知层能力（多模态信号与外部情报接入） | 待开发 |
| V5 | 完善治理层能力 + 初版风险报告 Agent（汇总风险并输出报告） | 待开发 |

## 5. V3 收口完善（必须完成）

1. 完整补齐前后端联调用例证据：`interrogate -> cancel`、`interrogate -> pass_secondary -> confirm`、`interrogate -> block_secondary`（已完成）。
2. 将当前性能指标写实（而非占位）：`precheck`、`secondary-check`（已完成），`LLM P95` 待单独采样。
3. 对二次质询错误口径做统一编码（重复提交、非 pending、未通过确认）（已完成）。
4. 固化 API 契约快照（字段和语义），作为后续版本演进输入（已完成）。

## 6. 后续开发步骤（建议执行顺序）

1. **V3-beta：取消交易全链路与 Trace 审计落地**
   - 统一取消交易流程步骤与状态边界（发起、确认、终止、不可逆规则）。
   - 补齐取消链路指标：发起率、成功率、取消后误确认拦截率、场景分布。
   - Trace 审计统一写入 MCP，提供可检索字段与最小复核视图。
2. **V4：MCP 感知层能力补全**
   - 完整接入多模态输入信号（设备、行为脉冲、文本上下文）。
   - 标准化外部情报 MCP 工具调用协议和超时/降级策略。
   - 完成感知层输入契约版本化，保证上层 Agent 可稳定消费。
3. **V5：治理层补全 + 初版风险报告 Agent**
   - 完善治理层规则编排、边界拒绝策略、审计字段一致性。
   - 新增初版风险报告 Agent：汇总当前风险因子、关键命中点、建议动作。
   - 输出结构化报告模板，支持人工复核与客服解释。

## 7. 开发 TODO（执行清单）

### P0（当前迭代）
- [x] 补齐 V3 联调场景自动化脚本（含 cancel/confirm/secondary 边界）。
- [x] 补齐性能基线采样并写入文档（真实值）。
- [x] 固化 API 契约快照（版本号 + 变更记录）。

### P1（V3-beta）
- [ ] 统一取消交易全链路步骤与状态约束。
- [ ] 建立取消交易指标看板字段（链路指标化）。
- [ ] Trace 审计写入 MCP 并提供查询接口。

### P2（V4）
- [ ] 完善 MCP 感知层输入信号接入与标准化契约。
- [ ] 完善外部 MCP 情报工具编排、超时和降级策略。
- [ ] 完成感知层能力回归（准确率/时延/稳定性）。

### P3（V5）
- [ ] 完善治理层策略引擎与审计一致性校验。
- [ ] 实现初版风险报告 Agent（风险汇总 + 结构化输出）。
- [ ] 打通报告查询与人工复核最小闭环。
