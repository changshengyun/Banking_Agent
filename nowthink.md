# nowthink - 当前架构思考与实施理由

> 本文档用于解释“为什么当前先这样实现”，不承担排期、范围定义或长期执行计划职能。当前执行以 `plan.md` 为准。

## 1. 核心愿景

目标是构建一个“知行合一”的金融级 Agent 风控大脑。不只判断用户说了什么，还要判断用户是如何作答、是否存在被教唆、背诵剧本、回避核验等信号。

## 2. 为什么当前不直接上 LangGraph

- 当前最重要的问题不是编排框架能力，而是决策质量是否可靠。
- 手写调度更容易调试、定位、对齐 Prompt 和接口语义。
- 在三类 Agent 的职责、输入输出和审计字段没有稳定之前，过早迁移框架会放大复杂度。

## 3. 为什么坚持三类 Agent 分工

### 领域初分

- 负责快速定性与首轮追问方向。
- 核心任务是把用户带入正确的风险分析分支。

### 领域细分

- 负责在具体诈骗剧本中做更深层匹配。
- 核心任务是识别场景深度，而不是泛化地再做一次分类。

### 语义分析

- 负责检测语义红旗、知行偏差、被教唆痕迹。
- 核心任务是做“测谎”和纠偏，而不是做场景路由。

## 4. 为什么 Trace 独立记录

- 决策链路的目标是稳定和低延迟。
- 审计链路的目标是完整和可复核。
- 将 Trace 独立写入日志或 MCP 通道，可以避免审计诉求拖累交易主链路。

## 5. 当前最关键的架构原则

- Agent 只提供分析与建议，不直接执行业务裁决。
- 治理层必须保留确定性控制权。
- 前端交互动作必须有明确业务语义，尤其是取消交易。
- Prompt 必须服务于职责边界，而不是替代治理层规则。

## 6. 当前阶段与未来阶段关系

- V3-alpha：手写调度、三类 Agent、向量语义检索。
- V3-beta：取消交易闭环、Trace 审计加强。
- V4：在状态字段、I/O 契约和轮次控制稳定后，评估迁移到图式编排框架。

---

整理日期：2026-04-07
