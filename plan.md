# 银行风控项目计划（当前唯一执行计划）

> 本文件是当前阶段唯一执行计划来源。若与 `MVP.md`、`BANK_V1/BANK_V1.md`、`nowthink.md`、`work_now.md` 冲突，以本文件为准。

## 1. 当前阶段

- 当前阶段：**MVP V3 / 交互与治理收口前置阶段**
- 当前目标：在不破坏现有主链路稳定性的前提下，完成文档治理、交互收口、多 Agent 职责正式化与阶段验收。
- 当前策略：继续采用 FastAPI + 手写调度 + 三类 Agent + 风险规则引擎，不引入 LangGraph/LangChain 作为主编排框架。

## 2. 当前架构

- 后端：FastAPI 提供预检、二次质询、确认转账及相关服务接口。
- 决策形态：Agent 只负责分析与建议，最终裁决由治理层规则引擎/状态机给出。
- Agent 分工固定为三类：
  - 领域初分：快速打标并生成初始追问方向。
  - 领域细分：针对已知诈骗剧本做更细粒度识别与话术比对。
  - 语义分析：识别被教唆、背诵剧本、回避核验、知行偏差等语义红旗。
- 风控主链路保持现有预检与二次质询接口契约稳定。
- Trace 作为独立日志流存在，不接入主决策编排。

## 3. 目标架构

- 长期目标仍是 HIRD 四层结构：
  - 感知集成层：多模态上下文与微观行为特征输入。
  - 认知决策层：多 Agent 分工协作。
  - 治理控制层：确定性裁决、PII 护栏、审计闭环。
  - 业务执行层：显式业务动作与结果展示。
- V4 才评估迁移到 LangGraph/LangChain 等图式编排框架。
- 编排迁移前必须先稳定当前手写链路的输入输出、状态字段、轮次控制和审计字段。

## 4. 本轮范围

- 文档治理：统一文档角色、优先级、阶段口径与验收口径。
- 交互治理收口：
  - Chat 无效输入统一提示：`无效输入，请按照要求输入`
  - 质询窗口增加显式“取消交易”入口
- 多 Agent 正式化：将三类 Agent 的职责、输入输出与禁止越权行为写成正式规范。
- 阶段验收：回归关键转账流程、质询流程、无效输入路径、取消交易路径。

## 5. 本轮非范围

- 不引入 LangGraph/LangChain 作为主编排框架。
- 不新增独立 Trace Agent。
- 不扩展新的感知层能力。
- 不新增新的风险场景专家类型。
- 不改变现有风险决策枚举与预检/质询接口语义。

## 6. 默认决策

- 当前主模型默认继续使用 DeepSeek-V3。
- 当前主编排默认继续使用手写调度。
- 三类 Agent 默认保持职责边界，不合并、不新增。
- Trace 默认独立写入日志或 MCP 通道，不介入主决策路径。
- `work_now.md` 默认只记录短周期任务快照，不承担长期策略定义。

## 7. 当前实施顺序

### 阶段 A：文档基线

1. 固化本文件的唯一执行计划地位。
2. 将 `MVP.md` 改写为目标蓝图文档，显式声明其不代表当前代码结构。
3. 将 `BANK_V1/BANK_V1.md` 收敛为历史归档文档。
4. 将 `nowthink.md` 收敛为架构意图与设计理由文档。
5. 将 `work_now.md` 收敛为本轮任务快照文档。

### 阶段 B：交互与治理收口

1. 统一无效输入策略。
2. 明确取消交易、普通关闭、正常完成三者的业务语义与边界。
3. 保持现有预检、质询、确认转账链路接口兼容。

### 阶段 C：多 Agent 正式化

1. 把三类 Agent 的职责、输入、输出、禁止越权行为整理成正式规范。
2. 将 `multagent_prompt.md` 提升为正式 prompt 来源文档。
3. 保持 prompt 与治理层边界一致。

### 阶段 D：阶段验收

1. 回归关键转账流程。
2. 回归前端关键交互。
3. 更新文档中的阶段状态，避免描述漂移。

## 8. 文档角色定义

- `plan.md`：当前唯一执行计划。
- `MVP.md`：目标蓝图与未来编排形态说明。
- `BANK_V1/BANK_V1.md`：历史归档、演进背景、遗留问题记录。
- `nowthink.md`：为什么当前这样实现的设计理由说明。
- `work_now.md`：本轮短周期任务快照，每完成一个子任务就更新，不承担长期策略定义。
- `multagent_prompt.md`：三类 Agent 的正式 prompt 来源与行为约束。

## 9. 验收标准

### 9.1 决策质量

- 风险分类结果与已知场景一致。
- 二次质询能区分正常解释与被教唆话术。
- 三类 Agent 不互相越权。
- 三类agent 输出的分析结果与治理层输入一致，得到的数据与数学公式一致，且能正确影响最终决策。
  - 场景专家输出 `c_match`
  - 语义分析输出 `s_dev`
  - 动态权重：
  - `w_adj = w_base + (1 - w_base) * c_match`
- 最终风险分：
  - `f_final = (1 - w_adj) * s_static + w_adj * s_dev`
- 路由原则：
  - 低风险直接放行
  - 高风险直接拦截
  - 中间风险进入受控追问，超过轮次则转人工
- 实现目标数学模型中定义的动态权重调整和最终风险分计算，并验证其在不同场景下的表现符合预期。

### 9.2 交互质量

- 用户能明确看到取消交易按钮。
- 用户在无效输入时得到一致提示。
- 质询流程不破坏现有确认链路。

### 9.3 文档质量

- 本文件能单独说明当前阶段、范围边界、默认决策和验收标准。
- `MVP.md` 不再被误读为当前架构说明。
- `BANK_V1/BANK_V1.md` 与 `nowthink.md` 不再承担当前执行约束。

## 10. 后续演进触发条件

### V3-beta

- 触发条件：
  - 当前手写调度稳定
  - 三类 Agent 边界稳定
  - 取消交易闭环完成
  - Trace 字段模型确定
- 目标：
  - 打通取消交易链路
  - 打通 MCP Trace 审计落库

### V4

- 触发条件：
  - 状态字段稳定
  - Agent I/O 稳定
  - 轮次控制稳定
  - 审计字段可测试
- 目标：
  - 评估并迁移至 LangGraph/LangChain 等图式编排框架
  - 实现更完整的状态机与多 Agent 编排
