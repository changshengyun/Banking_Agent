# HIRD 手机银行风控系统 MVP 目标蓝图

> 本文档描述目标架构与未来演进方向，不代表当前代码结构或当前执行策略。当前阶段的唯一执行计划以 `plan.md` 为准。

## 1. 文档定位

- 文档角色：**Target Architecture / 目标蓝图**
- 使用目的：描述系统理想形态、目标能力、未来编排方式与验收方向。
- 非当前实现声明：文中的 LangGraph 状态机、多节点编排、完整审计闭环均为目标态，不视为当前仓库已实现内容。

## 2. 目标系统愿景

本系统旨在构建一个基于 HIRD（Hierarchical Intent-Native Risk Defense）架构的手机银行全链路风控系统，实现从“规则拦截”向“意图识别”的演进。

目标不是只判断用户说了什么，而是结合上下文、业务场景和微观行为信号，识别用户是否处于被诱导、被教唆或被诈骗的风险状态。

## 3. 目标四层架构

### 3.1 感知集成层（HIRD-H）

- 汇聚设备、地域、账户状态、交易信息、行为脉冲等上下文。
- 采集输入停顿、退格频率、切屏次数、复制粘贴等微观行为。
- 对外部情报源、知识图谱与风险库进行标准化封装。

### 3.2 认知决策层（HIRD-C/R）

- 领域初步分类 Agent：
  - 快速进行场景打标
  - 生成初始质询方向
- 领域细分 Agent：
  - 对照具体诈骗剧本进行深度识别
  - 输出知识匹配置信度
- 语义分析 Agent：
  - 分析回复中的教唆痕迹、背诵感、规避核验与知行偏差
  - 输出语义红旗与偏差评分

### 3.3 治理控制层（HIRD-G）

- PII 脱敏作为强制门禁。
- 确定性规则引擎负责最终裁决。
- Trace 审计独立记录，不受 LLM 不确定性影响。
- 轮次控制与状态流转由可验证的状态机实现。

### 3.4 业务执行层（HIRD-E）

- 承接放行、拦截、转人工、取消交易等明确业务动作。
- 用户侧展示解释型节点与风险反馈。
- 管理侧提供结构化报告与审计线索。

## 4. 目标编排形态

- 目标编排框架：LangGraph 或等价图式状态机框架。
- 目标状态对象应至少覆盖：
  - `client_context`
  - `user_text`
  - `turn_count`
  - `f_final`
  - `risk_reasoning`
- 并行计算簇的目标形态：
  - 场景专家输出 `c_match`
  - 语义分析输出 `s_dev`
- 治理层根据确定性公式与轮次条件完成状态路由。

## 5. 目标数学模型

- 动态权重：
  - `w_adj = w_base + (1 - w_base) * c_match`
- 最终风险分：
  - `f_final = (1 - w_adj) * s_static + w_adj * s_dev`
- 路由原则：
  - 低风险直接放行
  - 高风险直接拦截
  - 中间风险进入受控追问，超过轮次则转人工

## 6. 目标验收口径

- 深度伪装场景在有限轮次内达到高阻断率。
- Prompt 注入或越权建议不能突破治理层硬规则。
- 在并行 Agent 调用场景下保持可接受时延。
- 风险报告具备可解释性与审计可复核性。

## 7. 与当前阶段的关系

- 当前阶段不按本文直接实施完整图式编排。
- 当前阶段只吸收本文中的目标能力、角色边界和演进方向。
- 真正的当前执行顺序、范围边界和默认决策以 `plan.md` 为准。
